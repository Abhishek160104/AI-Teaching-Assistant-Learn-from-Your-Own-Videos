# ============================================
# AI Teaching Assistant - Requirements
# ============================================

# ── Video to Audio Conversion ──────────────
ffmpeg-python==0.2.0          # Video → MP3 conversion

# ── Speech to Text (Transcription) ────────
openai-whisper==20231117      # MP3 → JSON transcription

# ── OpenAI GPT (LLM + Embeddings) ─────────
openai==1.30.0                # GPT-3.5 / GPT-4 + Embeddings API

# ── Vector Store & Data Processing ────────
joblib==1.3.2                 # Save/load embedding vectors
numpy==1.26.4                 # Numerical operations
pandas==2.2.2                 # Dataframe handling
scikit-learn==1.4.2           # Cosine similarity for retrieval

# ── Utility ───────────────────────────────
tqdm==4.66.4                  # Progress bars
python-dotenv==1.0.1          # Load API keys from .env file
tiktoken==0.7.0               # Token counting for OpenAI
